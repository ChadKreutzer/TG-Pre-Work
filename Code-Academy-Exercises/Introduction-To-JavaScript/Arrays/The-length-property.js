const objectives = ['Learn a new languages', 'Read 52 books', 'Run a marathon'];

console.log(objectives.length);

/* Kinda a review from using the length method earlier. Granted then it was
used on strings and I don't remember them explicitly saying you could use it
on Arrays too. */