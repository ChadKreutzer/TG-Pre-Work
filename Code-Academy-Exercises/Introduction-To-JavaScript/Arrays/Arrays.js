const newYearsResolutions = [
  'Keep a journal',
  'Take a falconry class',
  'Learn to juggle'
];

console.log("New Year's Resolutions:");
newYearsResolutions.forEach(
  (resolution, index) => console.log(`${index + 1}: ${resolution}`)
);

/* I don't see the difference. I think the log looks just like the real life
list. :D */