// Write your code below
const vacationSpots = ['New York City', 'London', 'Tokyo'];

console.log(vacationSpots[0]);
console.log(vacationSpots[1]);
console.log(vacationSpots[2]);

/* They are not wrong. It would be a pain, in fact nearly impossible, to
program if you didn't have access to looping. */
