const monitorCount = (rows, columns) => rows * columns;

const numOfMonitors = monitorCount(5, 4);

console.log(numOfMonitors);

/* For the fun of it, I made the function an expression and an arrow function. */